---
description: Installe et configure les prerequis de l'environnement Claude Code (MCP, hooks, CLI)
---

# Objectif

Lancer le script de configuration de l'environnement pour installer tous les prerequis du systeme.

## Instructions

1. Executer le script `scripts/env-setup.sh` via Bash
2. Afficher le resultat a l'utilisateur
3. Signaler les eventuels problemes detectes

## Execution

```bash
bash scripts/env-setup.sh
```

Si le script echoue, analyser l'erreur et proposer une solution.
